# SwiftPM
.build/
.swiftpm/

# Xcode
xcuserdata/
DerivedData/
*.xcresult

# Secrets — never commit the real Spotify client ID (see Secrets.example.plist)
DriveVerse/Resources/Secrets.plist

# macOS
.DS_Store

# Raw screen recording (kept local, not committed)
demo-raw.mp4
